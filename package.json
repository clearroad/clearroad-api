{
  "name": "clearroadlab-api",
  "version": "0.0.1",
  "description": "",
  "main": "dist/clearroad.js",
  "typings": "dist/clearroad.d.ts",
  "scripts": {
    "build": "rimraf dist && node_modules/typescript/bin/tsc -p tsconfig.build.json",
    "lint": "tslint \"src/**/*.ts\"",
    "lint:fix": "tslint --fix \"src/**/*.ts\"",
    "deploy": "node deploy.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server",
    "release": "standard-version",
    "postrelease": "git push origin master --follow-tags",
    "commitmsg": "commitlint -e $GIT_PARAMS"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@clearroad-bitbucket.westeurope.cloudapp.azure.com:7999/labs/clearroadlab-api.git"
  },
  "author": "Guillaume Royer <guillaumeroyer@gmail.com>",
  "license": "none",
  "devDependencies": {
    "@commitlint/cli": "^7.0.0",
    "@commitlint/config-conventional": "^7.0.1",
    "azure-storage": "^2.10.0",
    "dotenv": "^6.0.0",
    "http-server": "^0.11.1",
    "rimraf": "^2.6.2",
    "standard-version": "^4.4.0",
    "tslint": "^5.11.0",
    "typescript": "^2.9.2"
  }
}
